import React from "react";
import HolidayList from "../employee/HolidayList";

// PM view reuses the Employee Holiday List + Year Calendar UI
const HolidayCalendar = () => {
  return <HolidayList />;
};

export default HolidayCalendar;
